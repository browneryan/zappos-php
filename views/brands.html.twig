<!DOCTYPE html>
<html>
  <head>
    {% include '/includes/head.html.twig' %}
    <title>Zappos</title>
  </head>
  <body>
    <div class="container">
      <h3>Brands listed:</h3>
        {% if brands is not empty %}
          <ul>
            {% for brand in brands %}
              <a href="/brand/{{ brand.getId }}"><li>{{ brand.getName }}</li></a>
            {% endfor %}
          </ul>
            {% else %}
              <p>There are no brands listed...</p>
        {% endif %}
      <div class="add-features">
         <div class="row">
          <div class="col s12">
            <h5>Add Brand</h5>
            <form action="/add_brand" method="post">
              <label for="name">Brand name:</label>
              <input type="text" name="name" id="name">
              </br>
              <button type="submit" class="waves-effect waves-light btn-large" name="button">Submit</button>
            </form>
            <br>
            <form action="/delete_all_brands" method="post" class='form-group'>
              <button type="submit" class="waves-effect waves-light btn-large" name="button">Delete all</button>
            </form>
            <br>
            <a href="/" type="submit" class="waves-effect waves-light btn-large" name="button">Home</a>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
